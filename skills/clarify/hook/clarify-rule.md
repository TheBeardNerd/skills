Before acting on the user's message: if it is GENUINELY ambiguous or under-specified — you could reasonably build two or more materially different things from it, or you cannot state what "done" looks like — invoke the `clarify` skill before doing anything else.

Do NOT invoke `clarify` for: clear requests (even short ones), anything resolvable by reading the codebase or recent context, trivial edits, or simple questions. The bar is HIGH — brevity is not ambiguity. When unsure, proceed with briefly-stated assumptions rather than interrupting.
